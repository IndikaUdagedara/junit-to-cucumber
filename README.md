# JUnit to Cucumber JSON converter

Convert JUnit XML format to Cucumber JSON format. This is inspired by https://www.npmjs.com/package/cucumber-junit-convert which does the reverse of this.


## Install
```
npm install
npm install -g .
```


